# _archive

Files kept for reference but no longer wired into the active project. Safe to
delete later once confirmed unneeded.

## zenspa-booking/
Archived 2026-07-07. Contained only `firestore.customer.rules`, which was not
referenced anywhere (the deployed Firestore rules are `zenspa backend/firestore.rules`,
per `firebase.json`). Moved here instead of deleting in case the customer-scoped
rules are wanted for a future migration.

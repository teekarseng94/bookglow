/**
 * Keep this in lockstep with android/app/build.gradle versionName.
 * Shown on the Android boot screen so testers can confirm Play served the new AAB.
 */
export const ANDROID_APP_VERSION = '1.0.7';

/**
 * Point Transaction Service — Supabase only (Phase D).
 */
import * as supabaseMerchant from "./supabaseMerchant";

export const setCurrentOutletID = supabaseMerchant.setCurrentOutletID;
export const getCurrentOutletID = supabaseMerchant.getCurrentOutletID;
export const pointTransactionService = supabaseMerchant.pointTransactionService;
